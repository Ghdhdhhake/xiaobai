#include<stdio.h>
int main(void)
{/*
	int a, b;
	scanf_s("%d %d", &a, &b);
	int t;
	while (b != 0) {
		t = a%b;
		a = b;
		b = t;
	}
	printf("gcd=%d\n", a);
	return 0;*/
	/*int n;
	scanf_s("%d",&n);
	double dividend, divisor;
	int t;
	double sum = 0.0;
	dividend = 2;
	divisor = 1;

	for (int i = 1; i <= n; i++) {
		sum += dividend/divisor;
		t = dividend;
		dividend = dividend + divisor;
		divisor = t;
		}
	printf("%.2f\n",sum);
	return 0;*/
	
	}
}