#include<stdio.h>
int main()
{
	/*int a, b, t, r, m;
	scanf_s("%d %d", &a, &b);
	if (a < b) {
		t = a;
		a = b;
		b = t;
	}
	r = a % b;
	m = a * b;
	while (r != 0) {
		a = b;
		b = r;
		r = a % b;
	}
	printf("最小公倍数是%d,最大公约数是%d", b, m /b);
	return 0;*/


	/*int score;
	char grade;
	printf("Ener your score :");
	scanf_s("%d", &score);
	grade = score >= 90 ? 'A' : (score >= 60 ? 'B' : 'C');
	printf("Your grade is %c", grade);
	return 0;*/


	/*int m, a, b;
	b = 1;
	scanf_s("%d", &m);
	printf("%d = ", m);
	for (int i = 2; i <= m; i++) {
		while (m % i == 0)
		{
			printf("%d",i);
			m /= i;
			if (m != 1)
				printf("*");

		}
	}*/
}